# PLATEAU CityGML Importer for Revit
## トラブルシューティングガイド

**バージョン**: 1.0.0
**最終更新**: 2025年11月7日

---

## 目次

1. [インストール時の問題](#インストール時の問題)
2. [起動・表示の問題](#起動表示の問題)
3. [インポート時の問題](#インポート時の問題)
4. [パフォーマンスの問題](#パフォーマンスの問題)
5. [ジオメトリの問題](#ジオメトリの問題)
6. [座標・位置の問題](#座標位置の問題)
7. [エラーメッセージ一覧](#エラーメッセージ一覧)
8. [ログとデバッグ情報](#ログとデバッグ情報)

---

## インストール時の問題

### ❌ 問題: 「別のバージョンが既にインストールされています」

**原因**: 古いバージョンのアドインが残っている

**解決方法**:

1. **既存バージョンをアンインストール**
   - Windows 設定 → アプリ → 「PLATEAU CityGML Importer for Revit」を検索
   - アンインストールをクリック

2. **手動でファイルを削除**（アンインストールが失敗する場合）
   ```
   C:\Program Files (x86)\PlateauRevitImporter\
   %ProgramData%\Autodesk\Revit\Addins\2025\PlateauRevitImporter.addin
   %ProgramData%\Autodesk\Revit\Addins\2026\PlateauRevitImporter.addin
   %ProgramData%\Autodesk\Revit\Addins\2027\PlateauRevitImporter.addin
   ```

3. **再インストール**

---

### ❌ 問題: 「管理者権限が必要です」

**原因**: インストーラーの実行に管理者権限が不足

**解決方法**:

1. `PlateauRevitImporter.msi` を右クリック
2. **「管理者として実行」** を選択

---

### ❌ 問題: インストールが途中で止まる

**原因**: ウイルス対策ソフトがブロックしている可能性

**解決方法**:

1. ウイルス対策ソフトを一時的に無効化
2. インストールを再試行
3. インストール後、ウイルス対策ソフトを再度有効化
4. 例外リストに以下を追加:
   ```
   C:\Program Files (x86)\PlateauRevitImporter\
   ```

---

## 起動・表示の問題

### ❌ 問題: Revitに「PLATEAU」タブが表示されない

**原因1**: Revitが起動中にインストールした

**解決方法**:
1. Revit を**完全に終了**（タスクマネージャーでプロセスを確認）
2. Revit を再起動

---

**原因2**: .addinファイルが正しく配置されていない

**解決方法**:

1. 以下のフォルダに `PlateauRevitImporter.addin` があるか確認:
   ```
   C:\ProgramData\Autodesk\Revit\Addins\2025\
   C:\ProgramData\Autodesk\Revit\Addins\2026\
   C:\ProgramData\Autodesk\Revit\Addins\2027\
   ```

2. ファイルが存在しない場合、手動でコピー:
   - ソース: `C:\Program Files (x86)\PlateauRevitImporter\PlateauRevitImporter.addin`（ただし、インストーラーディレクトリからコピー）
   - 宛先: 上記のフォルダ

---

**原因3**: DLLファイルが見つからない

**解決方法**:

1. 以下のファイルが存在するか確認:
   ```
   C:\Program Files (x86)\PlateauRevitImporter\PlateauRevitImporter.dll
   ```

2. 存在しない場合は、アドインを再インストール

---

### ❌ 問題: 「CityGML インポート」ボタンをクリックしても反応しない

**原因**: アドインの初期化エラー

**解決方法**:

1. Revit を再起動
2. 「ファイル」 → 「オプション」 → 「一般」 → **「起動時にアドインをロード」** にチェックが入っているか確認
3. Revit ジャーナルファイルでエラーを確認:
   ```
   %LOCALAPPDATA%\Autodesk\Revit\<バージョン>\Journals\
   ```

---

## インポート時の問題

### ❌ 問題: 「ファイルを読み込めませんでした」

**原因1**: ファイル形式が正しくない

**解決方法**:
- ファイル拡張子が `.gml` であることを確認
- CityGML 2.0 形式であることを確認（CityJSON や IFC は非対応）

---

**原因2**: ファイルが破損している

**解決方法**:
- PLATEAU VIEW または G空間情報センターから再ダウンロード
- ZIPファイルを解凍し直す

---

**原因3**: ファイルパスに日本語が含まれている

**解決方法**:
- ファイルを英数字のみのパス（例: `C:\Temp\`）に移動
- 再度インポートを試行

---

### ❌ 問題: 「XML形式が不正です」

**原因**: CityGML 1.0 や CityJSON など、非対応形式のファイル

**解決方法**:
- PLATEAU VIEW でダウンロード時に **「CityGML 2.0」** を選択
- CityJSON形式は未対応（将来のバージョンで対応予定）

---

### ❌ 問題: 「建物データが見つかりません」

**原因**: 建物以外のフィーチャータイプのファイルを選択している

**解決方法**:
- ファイル名に `bldg` が含まれているか確認
- 例:
  - ✅ `53393652_bldg_6697_op.gml` ← 建物データ（OK）
  - ❌ `53393652_tran_6697_op.gml` ← 道路データ（未対応）
  - ❌ `53393652_brid_6697_op.gml` ← 橋データ（未対応）

---

### ❌ 問題: 進捗バーが0%で止まる

**原因**: 非常に大容量のファイル（200MB以上）で、初期解析に時間がかかっている

**解決方法**:
- そのまま待機（5～10分程度かかる場合があります）
- タスクマネージャーで Revit のCPU使用率が0%でない場合、処理中です

---

### ❌ 問題: 「メモリ不足」エラー

**原因**: 非常に大容量のファイルでメモリが不足

**解決方法**:

1. **ファイルを分割**
   - 地区ごとに分けてインポート
   - 必要な範囲のメッシュのみダウンロード

2. **Revitを再起動**
   - メモリをクリアしてから再試行

3. **PCスペックの確認**
   - 推奨: RAM 16GB以上
   - 最低: RAM 8GB

---

## パフォーマンスの問題

### ⚠️ 問題: インポートが非常に遅い（10分以上）

**原因1**: ファイルサイズが非常に大きい（300MB以上）

**解決方法**:
- LOD1で試してみる（LOD2より高速）
- ファイルを地区ごとに分割

---

**原因2**: PCスペックが不足

**解決方法**:
- 他のアプリケーションを閉じる
- Revit以外のプロセスを終了
- PCを再起動してメモリを解放

---

**原因3**: デバッグモードでビルドされている（開発者向け）

**解決方法**:
- Release ビルドを使用しているか確認
- Debug ビルドは大量のログ出力により遅くなります

---

### ⚠️ 問題: Revitの動作が重くなった

**原因**: 大量のDirectShapeがインポートされている

**解決方法**:

1. **不要なモデルを削除**
   - 3Dビューで選択 → Delete

2. **ビューフィルターで非表示**
   - 表示/グラフィックス → フィルター → 「PLATEAU Buildings」のチェックを外す

3. **ワークセットで管理**（大規模プロジェクトの場合）
   - ワークセットを作成し、PLATEAU モデルを別ワークセットに移動

---

## ジオメトリの問題

### ⚠️ 問題: 建物が表示されない、または一部しか表示されない

**原因1**: ビューの範囲外に配置されている

**解決方法**:
1. 3Dビューで **「ズームフィット」**（ZF）を実行
2. 「すべて表示」（ZA）を実行

---

**原因2**: ビューフィルターで非表示になっている

**解決方法**:
1. 「表示/グラフィックス」を開く
2. 「フィルター」タブで **「PLATEAU Buildings」** にチェックが入っているか確認

---

**原因3**: GMLファイルに不正なジオメトリが含まれている

**解決方法**:
- 完了メッセージの **「エラー件数」** を確認
- エラー件数が多い場合、データ提供元に確認

---

### ⚠️ 問題: 建物の形状がおかしい（欠けている、歪んでいる）

**原因**: GMLデータの品質問題（PLATEAU側のデータ品質）

**解決方法**:
- **LOD1とLOD2を切り替えて比較**
- LOD2で問題がある場合、LOD1を使用
- データ提供元（PLATEAU）に報告

---

### ⚠️ 問題: 建物が重複している

**原因**: 同じファイルを2回インポートした

**解決方法**:
1. 重複したモデルを手動で削除
2. 削除方法:
   - 3Dビューで選択 → Delete
   - または「選択」 → 「カテゴリ別」 → 「PLATEAU Buildings」 → Delete

---

## 座標・位置の問題

### ⚠️ 問題: 建物が思った位置に配置されない

**原因**: 固定参照点方式により、すべてのモデルは参照点（lat=35.629°, lon=139.781°）からの相対座標で配置される

**解決方法**:
- **正常な動作です**
- 複数地区をインポートしても、相対的な位置関係は正確です
- Revit内での絶対座標は意味を持ちません（相対位置のみ重要）

---

### ⚠️ 問題: 2回目のインポートで位置がズレた

**原因**: 通常はズレません。以下を確認:

**確認方法**:
1. 既存のモデルを削除していないか確認
2. 異なる座標系のファイルを混在させていないか確認（すべてEPSG:6697であるべき）

**解決方法**:
- すべてのモデルを削除し、最初からインポートし直す

---

## エラーメッセージ一覧

### 🔴 「CityGMLファイルの解析中にエラーが発生しました」

**原因**: XML解析エラー、ファイル破損

**解決方法**:
- ファイルを再ダウンロード
- テキストエディタでXML構文エラーを確認

---

### 🔴 「座標変換中にエラーが発生しました」

**原因**: 座標データが不正、または範囲外

**解決方法**:
- ファイルの座標系が EPSG:6697 であることを確認
- 日本国外のデータは未対応

---

### 🔴 「ジオメトリ生成中にエラーが発生しました」

**原因**: Revit APIのジオメトリ生成エラー

**解決方法**:
- Revit を再起動
- LOD1とLOD2を切り替えて試す
- 問題が継続する場合、データ品質の問題

---

### 🔴 「トランザクションのコミットに失敗しました」

**原因**: Revitのトランザクション競合

**解決方法**:
- インポート中に他の操作をしない
- Revit を再起動

---

## ログとデバッグ情報

### Revit ジャーナルファイル

エラーの詳細情報は Revit ジャーナルファイルに記録されます：

**場所**:
```
C:\Users\<ユーザー名>\AppData\Local\Autodesk\Revit\Autodesk Revit <バージョン>\Journals\
```

**確認方法**:
1. 最新の `.txt` ファイルを開く
2. `PLATEAU` または `PlateauRevitImporter` で検索
3. エラーメッセージやスタックトレースを確認

---

### デバッグビルド（開発者向け）

開発環境でデバッグビルドを使用すると、詳細なログが出力されます：

1. Visual Studio で **Debug モード** でビルド
2. **Output ウィンドウ** で `Debug` を選択
3. CityGML解析、座標変換の詳細ログを確認

例:
```
CityGML生座標（緯度・経度） #1: 緯度=35.6895000, 経度=139.6917000, 高さ=10.50m
変換後相対座標（メートル） #1: X=6789.12m, Y=-10234.56m, Z=10.50m
```

---

## 問題が解決しない場合

### サポート窓口

- **メール**: contact@tutti-works.com
- **GitHub Issues**: [プロジェクトリポジトリ](#)（公開予定）

### 報告時に必要な情報

以下の情報を含めてください：

1. **Revitバージョン** (例: Revit 2026)
2. **OSバージョン** (例: Windows 11 Pro)
3. **GMLファイル情報** (ファイル名、サイズ、ダウンロード元)
4. **エラーメッセージ** (スクリーンショット)
5. **Revit ジャーナルファイル** (該当箇所の抜粋)
6. **再現手順** (1. ～ 順番に記載)

---

**© 2025 tutti-works**
**連絡先**: contact@tutti-works.com
